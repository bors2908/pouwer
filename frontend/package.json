{
  "name": "pow-frontend",
  "version": "0.1.0",
  "description": "Proof of Work Frontend Implementation",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs"
  },
  "dependencies": {
    "js-sha256": "^0.11.1",
    "randomx.js-shared": "0.1.3-m"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.0.0"
  }
}

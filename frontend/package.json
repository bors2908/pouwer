{
  "name": "pow-frontend",
  "version": "0.1.0",
  "description": "Proof of Work Frontend Implementation",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "check:contracts": "node ./scripts/check-contract-parity.cjs",
    "build": "npm run check:contracts && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "js-sha256": "^0.11.1",
    "randomx.js-shared": "0.1.3-m"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}

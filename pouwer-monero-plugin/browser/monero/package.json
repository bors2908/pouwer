{
  "name": "@pouwer/bundle-monero",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pouwer/widget-traefik": "0.1.9",
    "@pouwer/worker-monero": "0.1.9"
  }
}
